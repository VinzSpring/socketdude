import Project from './project';

export default class ProjectLoader {
    public static loadProjects(path: string): Project[] {
        // TODO impl
        throw new Error('not impl');
    }

    public static saveProjects(path: string, projects: Project[]) {
        // TODO impl
        throw new Error('not impl');
    }
}
