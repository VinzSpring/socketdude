import Project from './project';

export default class ProjectLoader {
    static loadProjects(path: string): Project[] {
        //TODO impl
    }

    static saveProjects(path: string, projects: Project[]) {
        //TODO impl
    }
}